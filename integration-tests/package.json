{
  "name": "pixerve-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests and usage examples for Pixerve image processing server",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/test.js",
    "test:watch": "npm run build && node dist/test.js --watch",
    "example": "npm run build && node dist/example.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "image-processing",
    "integration-tests",
    "pixerve",
    "typescript"
  ],
  "author": "Pixerve Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "form-data": "^4.0.0",
    "jose": "^5.0.0"
  }
}