# Pixerve Configuration Example

# Data directory for databases (credentials, success, failures)
# Default: ./data
PIXERVE_DATA_DIR=./data

# Directory for direct file serving (where processed files are stored)
# Default: ./serve
PIXERVE_SERVE_DIR=./serve
